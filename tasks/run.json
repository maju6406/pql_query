{
    "puppet_task_version": 1,
    "supports_noop": false,
    "description": "Task to run pql query",
    "parameters": {
        "query": {
            "description": "pql query you want to execute.",
            "type": "String[1]"
        },
        "store_results": {
            "description": "Store results on disk. Defaults to yes.",
            "type": "Optional[Enum[yes, no]]"
        },
        "jsonlint": {
            "description": "Send results to jsonlint.com. Defaults to no. Automatically set to no if store_results is disabled",
            "type": "Optional[Enum[yes, no]]"
        }
    }
}